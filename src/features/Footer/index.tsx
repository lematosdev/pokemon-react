import styled from 'styled-components';

const StyledFooter = styled.footer`
  width: 100%;
  height: 248px;
  background-color: #161b22;
`;

const Footer: React.FC = () => {
  return <StyledFooter> Hola putita </StyledFooter>;
};

export default Footer;
